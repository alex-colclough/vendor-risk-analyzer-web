# Application
DEBUG=true
ENVIRONMENT=development

# AWS Bedrock API Key (Recommended - simplest authentication method)
# Create an API key at: https://console.aws.amazon.com/bedrock/home#/api-keys
AWS_BEARER_TOKEN_BEDROCK=your_bedrock_api_key_here
AWS_REGION=us-east-1

# Alternative: IAM Credentials (if not using API key)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_ROLE_ARN=arn:aws:iam::123456789012:role/your-role

# Bedrock Configuration (use inference profile ID with geographic prefix for API key auth)
BEDROCK_MODEL_ID=us.anthropic.claude-3-5-sonnet-20241022-v2:0
BEDROCK_MAX_TOKENS=4096
BEDROCK_TEMPERATURE=0.3
BEDROCK_TIMEOUT=60

# Security
# SECRET_KEY will be auto-generated if not set
# CORS_ORIGINS=["http://localhost:3001"]

# File Upload
UPLOAD_DIR=/tmp/analyzer-uploads
MAX_FILE_SIZE_MB=100
MAX_TOTAL_SIZE_MB=500
